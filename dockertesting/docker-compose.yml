services:
  mongoimgtest:
    image: mongo
    restart: always
    container_name: mongoimgtest
    ports:
      - "27017:27017"
    volumes:
      - ../storagedb:/data/dbstorage
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example

volumes:
  storagedb: